{
  "version": 1,
  "description": "Route /api/* and /portal/* deep routes through Cloudflare Pages Functions. Static portal assets are excluded so the CDN serves them directly.",
  "include": ["/api/*", "/portal/*"],
  "exclude": ["/portal/", "/portal/index.html", "/portal/assets/*", "/portal/vite.svg"]
}
